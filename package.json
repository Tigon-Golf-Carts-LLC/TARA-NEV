{
  "name": "tara-nev-site",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "description": "Static marketing site for TARA Neighborhood Electric Vehicles, deployed to GitHub Pages.",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "pnpm --filter @workspace/tara-ev run dev",
    "build": "pnpm --filter @workspace/tara-ev run build",
    "preview": "pnpm --filter @workspace/tara-ev run serve",
    "typecheck": "pnpm --filter @workspace/tara-ev run typecheck"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
